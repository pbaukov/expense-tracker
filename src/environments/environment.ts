export const environment = {
  production: false,
  dropboxAppKey: 'YOUR_APP_KEY',  // dropbox.com/developers/apps → Settings → App key
  dropboxRedirectUri: 'http://localhost:4200',
  dropboxFilePath: '/expenses.csv',
  syncIntervalMs: 60000
};
